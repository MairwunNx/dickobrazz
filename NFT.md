# NFT для Dickobrazz 🌶️🪙
Краткий документ о том, **как внедряем NFT** в проект: идея, сервисы, структура, тексты и тестирование.

---

## 1. Идея 🎯
**NFT — это коллекционные награды** за редкие достижения и сезонные победы.  
Цель — дать игрокам **публичные трофеи**, которые можно показать и при желании продать на маркетплейсе.

Основные принципы:
- **Нет влияния на гонки/соревнования** — чисто статус и коллекция
- **Публичная сеть**, чтобы NFT был виден вне бота
- **Автоматическая выдача** из нашего бэка по адресу кошелька

---

## 2. Описание задачи 🧩
Нужно сделать:
1. Привязку кошелька к пользователю в боте
2. Генерацию метаданных NFT и изображений
3. Минт NFT **на адрес кошелька** при достижениях
4. Отображение и ссылки на маркетплейсы
5. Тестовый контур для проверки минтов

---

## 3. Сервисы и блокчейн 🛠️
Требования: **публичные NFT**, возможность **минтить из кода** по адресу кошелька.

### Рекомендуемый стек (минимум боли)
- **Сеть**: `Polygon` (дёшево, массовые маркетплейсы)
- **Минт API**: `thirdweb` (простое API, админ‑ключ)
- **Хранилище метаданных**: `IPFS` через `NFT.Storage` или `Pinata`
- **Маркетплейс**: `OpenSea` (видимость, secondary‑рынок)

### Альтернативы
| Что | Вариант |
|---|---|
| API для минта | `Crossmint`, `Moralis`, `Alchemy` |
| Сеть | `Base`, `Arbitrum`, `Solana` |
| Маркетплейс | `Magic Eden`, `Rarible`, `OKX` |

**Почему thirdweb**: можно выдать NFT по адресу без сложного Web3 кода, есть админ‑ключ.

### Endpoints (thirdweb Engine/REST) 🌐
Ниже **ориентиры** по эндпоинтам (конкретные пути и поля уточняем по актуальной документации):

| Действие | Примерный эндпоинт | Метод |
|---|---|---|
| Минт NFT на адрес | `POST /contract/{chain}/{contractAddress}/erc721/mint-to` | `POST` |
| Получить NFT по `tokenId` | `GET /contract/{chain}/{contractAddress}/erc721/get` | `GET` |
| Листинг NFT коллекции | `GET /contract/{chain}/{contractAddress}/erc721/get-all` | `GET` |
| Статус транзакции | `GET /transaction/{chain}/{txHash}` | `GET` |

**Авторизация** обычно через заголовок: `Authorization: Bearer <API_KEY>`.

---

## 4. NFT: состав, метаданные и типы 🧾

### Структура метаданных (пример)
```json
{
  "name": "Кубок: Сезон 1 (2025)",
  "description": "Награда за победу в первом сезоне 2025 года",
  "image": "ipfs://<CID>/season-2025-cup.png",
  "attributes": [
    { "trait_type": "Сезон", "value": "1" },
    { "trait_type": "Год", "value": "2025" },
    { "trait_type": "Категория", "value": "Победа" },
    { "trait_type": "Редкость", "value": "Легендарная" }
  ]
}
```

### Типы NFT
**Сезонные победы**
- `Кубок: Сезон <N> <YYYY>` (топ‑1)
- `Медаль: Сезон <N> <YYYY>` (топ‑3)
- `Топ‑13 сезона` (редкая серия)

**Сложные достижения**
- `Легенда сезона` (серия побед)
- `Марафон` (30 дней подряд)
- `Супер‑рандом` (крайние значения)

**Стаж**
- `1 год службы`, `2 года службы`, `3 года службы`

---

## 5. Клиентский код: привязка кошелька и команды 🤖

### Привязка кошелька
Лучше делать через **подпись сообщения**:
1. Пользователь вызывает команду `"/wallet connect"`
2. Бот отправляет строку для подписи:  
   `Подтвердите владение адресом для Dickobrazz: <nonce>`
3. Пользователь подписывает в кошельке и присылает подпись
4. Бэкенд проверяет подпись и сохраняет адрес

### Команды бота
- `"/wallet connect"` — привязать кошелёк  
- `"/wallet status"` — показать текущий адрес  
- `"/nft list"` — показать полученные NFT  
- `"/nft marketplace"` — ссылки на маркетплейсы

### Пример текстов сообщений
- Успех привязки:  
  **Кошелёк привязан.** Теперь награды будут прилетать прямо на него. 🪙
- Выдача NFT:  
  **Награда получена!** Твой NFT уже в кошельке.  
  Ссылка на маркетплейс будет доступна через пару минут. 🏆
- Ошибка:  
  **Не удалось проверить подпись.** Попробуй ещё раз.

### Зависимости
- `github.com/ethereum/go-ethereum` — проверка подписей
- HTTP‑клиент для `thirdweb` API
- Модуль хранения связок `user_id -> wallet`

---

## 6. Организационное: где храним картинки и метаданные 🗂️
**Картинки**:
- Рендерим заранее и складываем в `IPFS`
- Резервная копия на `S3/MinIO`

**Метаданные**:
- JSON в `IPFS`
- В БД хранить `tokenId`, `txHash`, `wallet`, `nft_type`

**Сборка изображений**:
- Можно хранить PNG‑шаблоны и подставлять текст сезона/даты
- Итоговые картинки публикуем в IPFS перед минтом

---

## 7. Тестирование (тестовый контур) ✅
1. Поднять тестовую сеть (`Mumbai` для Polygon)
2. Использовать отдельный API‑ключ thirdweb (test)
3. Выдать тест‑NFT на тестовый кошелёк
4. Проверить:
   - NFT отображается в кошельке
   - NFT виден на маркетплейсе тестнета
   - Ссылки из бота ведут на правильную карточку

**Проверка перевода**:
- В логах хранить `txHash`
- Проверять через публичный обозреватель (`polygonscan` testnet)
- Сверять `tokenId` + `wallet` в БД

---

## 8. Архитектура и структура Go‑кода 🧱
**Пакет**: `application/nft`

**Файлы**:
- `application/nft/service.go` — бизнес‑логика: минт, статусы
- `application/nft/metadata.go` — генерация/формирование JSON
- `application/nft/thirdweb.go` — клиент API thirdweb
- `application/nft/wallets.go` — привязка и проверка подписи
- `application/nft/types.go` — структуры данных и enum‑типы

**Интеграция**:
- Хендлеры команд в `application/telegram.go`
- Хранение в Mongo: коллекция `nft_mints`
- Кеш статусов выдачи в Redis (опционально)

---

## Дополнительно: ссылки, которые будем отдавать пользователю 🔗
- Ссылка на NFT в маркетплейсе (`OpenSea`)
- Ссылка на транзакцию (`polygonscan`)

Если нужно, оформлю следующий шаг:  
**конкретные поля коллекции, эндпоинты API, и скелет кода**.
